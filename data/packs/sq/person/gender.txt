---
description: Gender label (Albanian). Two values, matching en, pl and ru — this list is what a parent= filter matches on, so a longer one silently leaves most rows without a name. Identity options beyond the pair belong in a file of their own.
locale: sq
---
Mashkull
Femër
