---
description: Gender label (Spanish). Two values, matching en and ru — this list is what
  `parent="Gender.Hombre"` filters on, so a longer one silently leaves most rows without a
  name. Identity options beyond the pair belong in a file of their own.
locale: es
---
Hombre
Mujer
