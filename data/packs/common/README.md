# common — данные без языка

Здесь лежат генераторы и списки, которые **не зависят от языка** — общие для
всех: числовые вещи, коды, паттерны и т.п.

Адрес: `common.<...>` — например `common.color.name`. Первый сегмент `common`
**абсолютный** (не зависит от `<env local>`), как и имена стран.
