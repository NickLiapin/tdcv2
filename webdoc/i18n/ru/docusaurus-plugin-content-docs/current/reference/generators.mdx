---
title: Генераторы
sidebar_position: 4
---

# Справочник генераторов

Каждый `type` для [`<gen>`](../generators/overview.mdx). Каждая строка ведёт на свою полную страницу.

| `type`                                                  | Что производит                                                |
| :------------------------------------------------------ | :------------------------------------------------------------ |
| [`text`](../generators/text.mdx)                        | Значение из набора — равномерно или по точным `percent`       |
| [`number`](../generators/number.mdx)                    | Целое число в диапазоне или строку цифр фиксированной длины   |
| [`template`](../generators/template.mdx)                | Встроенные правдоподобные данные и технические идентификаторы |
| [`file`](../generators/file.mdx)                        | Значения из ваших файлов и CSV-колонок                        |
| [`date`](../generators/date.mdx)                        | Дату или дату-время в диапазоне и формате                     |
| [`symbol`](../generators/symbol.mdx)                    | Строку из набора символов или именованного алфавита           |
| [`regex`](../generators/regex.mdx)                      | Строку по конечному регулярному выражению                     |
| [`advanced_regex`](../generators/advanced-regex.mdx)    | Regex плюс взвешенный выбор между вариантами                  |
| [`increment` / `decrement`](../generators/counters.mdx) | Возрастающий и убывающий счётчики                             |
| [`timeseries`](../generators/timeseries.mdx)            | Временной ряд — тренд + сезонность + шум                      |
| [`pattern`](../generators/pattern.mdx)                  | Распределение по нарисованной кривой                          |
| [`http`](../generators/http.mdx)                        | Значение от вашего сервиса, по HTTP                           |
| [`pool`](../pools/overview.mdx)                         | Один целый член `<pool>` — запись, а не значение              |
| [`running`](../generators/running.mdx)                  | Итог, накопленный по колонке, а не разыгранный                |
| [`stat`](../generators/stat.mdx)                        | Одно число на весь прогон, в каждой строке                   |

## Сквозные атрибуты

Работают на **любом** генераторе (см. [Маски и регистр](../guides/masks-and-case.mdx)):

- `case=` / `mask=` — регистр букв и маски отображения;
- `missing=` — оставить часть ячеек пустыми.

Этим двум нужно, чтобы генератор выдавал что-то, к чему их вообще можно применить;
в остальных случаях они игнорируются:

- `order=` / `cycle=` — порядок значений (по умолчанию случайный или `sequential`).
  Он что-то обходит, поэтому работает на [`text`](../generators/text.mdx),
  [`file`](../generators/file.mdx) и [`date`](../generators/date.mdx) — диапазон дат
  обходится по шагу. [`number`](../generators/number.mdx) берёт значение из диапазона,
  обходить ему нечего, и там атрибут отвергается ([TDC015](errors.mdx)).
- `anomaly=` — вытолкнуть часть значений за пределы диапазона, умножив их. Правило
  про **значение**, а не про генератор: умножается всё, что читается как число, в том
  числе числовая строка из [`text`](../generators/text.mdx),
  [`file`](../generators/file.mdx) или пака. Всё остальное — имя, город — проходит без
  изменений, потому что «ещё дальше» для него не существует. А список, в котором чисел нет
  вовсе, отвергается ([TDC243](errors.mdx)): выталкивать за диапазон там нечего, и атрибут
  ничего не делал бы ни на одной строке.
  См. [Аномалии и пропуски](../guides/anomalies.mdx).

См. также [Обзор генераторов](../generators/overview.mdx).
