# docs/ is generated by webdoc/scripts/export-markdown.mjs and must stay
# byte-for-byte what that script produces — the drift check compares them
# exactly. Reformatting it would make the check fail on the next commit.
/docs/

# Working notes, kept out of the repository.
/temp_docs/

# Generated by typescript/scripts/generate-quick-types.mjs — 3957 addresses, one
# property each. The drift check compares it byte for byte against what the
# generator writes, and prettier unquotes property names it considers valid
# identifiers, which is enough to make the check fail on the next commit.
/typescript/src/quick/addresses.ts

# Generated by scripts/generate-examples.mjs from data/examples — the bodies are
# the three worked-example files verbatim. Prettier rewraps the long string
# literals, and `examples:check` compares byte for byte against what the
# generator writes, so a formatted copy fails the very next commit. Same trap as
# the addresses table above.
/typescript/src/cli/examples.generated.ts

# Generated by webdoc/scripts/build-pack-catalogue.mjs from data/bundles.json.
# Prettier expands the short arrays it writes, and `docs:check` compares against
# what the generator produces, so a formatted copy fails the very next commit.
# Third instance of the same trap in this file.
/webdoc/src/data/pack-catalogue.json
